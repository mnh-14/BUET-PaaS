apiVersion: v1
kind: ServiceAccount
metadata:
  name: paas-deployer
  namespace: buet-paas-system-team23
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: paas-deployer-cluster-role
rules:
  - apiGroups: [""]
    resources: ["namespaces", "services", "pods", "configmaps", "secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: paas-deployer-binding
subjects:
  - kind: ServiceAccount
    name: paas-deployer
    namespace: buet-paas-system-team23
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: paas-deployer-cluster-role
---
# The real secret values are injected separately via a local .env + shell script,
# so they are not committed into Git or the repo manifest.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: paas-deployer
  namespace: buet-paas-system-team23
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paas-deployer
  template:
    metadata:
      labels:
        app: paas-deployer
    spec:
      serviceAccountName: paas-deployer
      containers:
        - name: paas-deployer
          image: 192.168.128.152/paas-builders/deploy-service-image:v1.6
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              name: http
          envFrom:
            - secretRef:
                name: paas-deployer-env
          env:
            - name: PORT
              value: "5000"
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: paas-deployer
  namespace: buet-paas-system-team23
spec:
  selector:
    app: paas-deployer
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: paas-deployer-ingress
  namespace: buet-paas-system-team23
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: deployment-service.buet-paas-system-team23.192.168.64.121.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: paas-deployer
                port:
                  number: 80
    - host: deployment-service.buet-paas-system-team23.192.168.128.200.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: paas-deployer
                port:
                  number: 80
